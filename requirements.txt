# ─────────────────────────────────────────────────────────────────────────────
# requirements.txt
# For: 3_prompt_chaining.py  &  5_UPSC_essay_workflow.py
#
# Install: pip install -r requirements.txt
# Get Groq API key free at: https://console.groq.com
# ─────────────────────────────────────────────────────────────────────────────

# LangGraph — graph-based agent/workflow orchestration
langgraph>=0.2.0

# LangChain Groq integration — ChatGroq class
langchain-groq>=0.1.9

# LangChain core — BaseModel, messages, etc.
langchain-core>=0.2.0

# Pydantic — EvaluationSchema structured output (used in 5_UPSC_essay_workflow.py)
pydantic>=2.0.0

# Environment variable loader — load_dotenv()
python-dotenv>=1.0.0
